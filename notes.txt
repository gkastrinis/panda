TODO
====
check order of rules, predicates etc
global relations with @past?
annotation names in grammar


TEST
====
check in LB/Souffle
t2.logic -> unused
no @entity Animal -> int
empty propagation list
assert to errors

entities isolated in components
@past in rule head only in global (and not in constructor)
multiple components into component



MISC
====
ord in souffle
inline predicates
forAll
foo(x, y, z){2,1,3}
predicate graph
constraints
init optimize news?
consistent actors style

foo.clone(["x":1, "y":2])
// Create a copy of the current object. `m` has pairs of field x value (groovy style)
Relation copy(Map m, boolean duplicateRest = true) { throw new UnsupportedOperationException() }