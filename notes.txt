

// Foo(x) -> Animal(x).
// Bar(x) <- Foo(x), x != "cat".
// duplicate declarations
// duplicate rules
// (Animal)? x string x Animal
//FooBar(x, y, z) <- Ble(y, z), Baz(z), x != z.

//Bar__pArTiAl(x) <- x = 1.

// Pred(x, y) -> AP2(x), string(y).
// Pred(x, y) <- byVar[y] = x. // error