P("hello")
Q(x, 42) <- P(x)

component First {
	Z(x) : int
	P11(x) : string
}
as S1

component Base <X> {
	P1(x) <- P@X(x)
	P1(x) : string
}
Base <_> as S2

First as S3

component Core <T, G> : Base <G>  {
	P2(x) <- Q@G(x, _)
	P3(x) <- Q@G(_, x)
	W(x) <- Z@T(x)
}

Core<S3, _> as S4

component CoreA <X, Y> : Core<Y, X> {
	ABC(x) <- Q@X(x, _)
}

CoreA<_, S3> as S5

//Pred(x) <- W(x)

component Foo<T1, T2> {
	P1(x) <- P@T2(x)
	P1(x) <- P11@T1(x)
	P1(x) : string
}

Foo<S3, _> as B1
