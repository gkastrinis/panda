P(x) : int

component Base<A, B> {
    AA(x) <- P@A(x)
    BB(x) <- P@B(x)
}

component Foo<T> : Base<T, T> {
    P(x) <- P@T(x)
}

Foo<_> as F1

P(x) <- P@F1(x)

component Bar {
    Q(x) <- P(x), x > 10
    P(x) <- Q(x), x < 100
}

Bar as B1

Foo<B1> as F2

P(x) <- P@F2(x)