component Foo {
	P(x) <- Q(x).
	R(x) <- P(x).
	W(x) -> string(x).
	@type(capacity = 100000) Animal
	@Constructor
    cons:An[x] = y -> string(x), Animal(y).

    cons:An[x] = y new Animal, foo(y) <- x := "cat".
}
as S1

component Bar {
	R(x) <- Q(x).
}
as S2

Q(1).
Q(2).

S1 {*} -> S2
S2 {*} -> .
