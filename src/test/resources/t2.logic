@Output @Type Animal
@Type AP

@Type Cat <: Animal
@Type Dog <: Animal
@Type PersianCat <: Cat

@Output
Foo(x, y) : int, string

Foo(y, x) <- x = "J", y = 1

@Output @Type @Output Bar

@Output {
	FooBar(x, y) : int, string

	@Type Baz
}

@Output @Type {
	@Output Dog1
	Cat1
}

@Input {
	P1(x) : string
	P2(x) : int
	P3(x) : string
}

FooBar(42, "bla")
