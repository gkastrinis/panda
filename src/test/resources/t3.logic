@Entity Animal
@Entity AP

@Entity Cat -> Animal
@Entity Dog -> Animal
@Entity PersianCat -> Cat

@Output
Foo(x, y) -> int(x), string(y).

Foo(y, x) <- x = "J", y = 1.

@Output, @Entity, @Output Bar

@Output {
	FooBar(x, y) -> int(x), string(y).

	@Entity Baz
}

@Output, @Entity {
	@Output Dog1
	Cat1
}

@Input {
	P1(x) -> string(x).
	P2(x) -> int(x).
	P3(x) -> string(x).
}


@Entity T

@Constructor(refmode=true)
tref[x] = y -> string(x), T(y).

TPred(x, y) <- FooBar(_, x), tref[x] = y.
