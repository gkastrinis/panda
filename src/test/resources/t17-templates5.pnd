template FooBar<X, Y> : Baz<Y, Y> {
    P(x) <- Q@X(x), R@Y(x)
}

template Foo<E, F> : Baz<F, E> {
    P3(x) <- Q@E(x), R@F(x)
}

template Baz<C, D> : Bar<D, C> {
    P2(x) <- Q@D(x), R@C(x)
}

template Bar<A, B> {
    P1(x) <- Q@A(x), R@B(x)
}


template Base1 {
    Q(x) : int
}
Base1 as B1

template Base2 {
    R(x) : int
}
Base2 as B2

Foo<B1, B2> as FFFF
